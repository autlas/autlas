// Layout constants shared between App, Sidebar and ScriptDetailPanel.
// Centralized so a single edit propagates to every place that reserves
// space for the panel/sidebar/tree during resize coordination.

export const DETAIL_PANEL_MIN_WIDTH = 340;
export const DETAIL_PANEL_DEFAULT_WIDTH = 420;

export const SIDEBAR_MIN_WIDTH = 200;
export const SIDEBAR_COLLAPSED_WIDTH = 80;

export const TREE_MIN_WIDTH = 500;
